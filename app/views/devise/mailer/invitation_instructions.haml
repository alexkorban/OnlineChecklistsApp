%p
  Hello #{@resource.safe_name}!
%p
  You have been invited to
  = succeed "." do
    = link_to "OnlineChecklists", root_url

%p You can set your password and sign in via the link below:

%p= link_to 'Set password and sign in', accept_invitation_url(@resource, :invitation_token => @resource.invitation_token)
%p
  If you don't want to accept the invitation, please ignore this email.
  %br/
  Your account won't be created until you access the link above and set your password.
