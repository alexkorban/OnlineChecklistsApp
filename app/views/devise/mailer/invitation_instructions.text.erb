Hello <%= @resource.safe_name %>!

You have been invited to OnlineChecklists (<%= root_url %>).

You can set your password and sign in via the link below:
<%= accept_invitation_url(@resource, :invitation_token => @resource.invitation_token) %>

If you don't want to accept the invitation, please ignore this email.

Your account won't be created until you access the link above and set your password.
